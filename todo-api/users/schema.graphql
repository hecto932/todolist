type User {
  _id: ID!
  name: String!
  email: String!
  tasks: [Task!]
  createdAt: Date
  updatedAt: Date
}

type Query {
  "Return all the users"
  getUsers: [User!]
}

type Mutation {
  "Request token"
  userLogin(email: String, password: String): JSON
}